-- Coq Settings (needs to be set before coq loads)
vim.g.coq_settings = {
  auto_start = 'shut-up',
  keymap = {
    jump_to_mark = '<C-m>'
  },
  display = {
    icons = {
      mode = 'none',
    }
  },
}

